name: goe-pv-steuerung

services:
  goe-pv-steuerung:
    build: .
    restart: unless-stopped
    ports:
      - "2009:2009"
    volumes:
      # Persistierung der Konfiguration, Ersparnis-Statistik & SSL Zertifikate
      - ./config.json:/app/config.json
      - ./savings.json:/app/savings.json
      - ./certs:/app/certs
    environment:
      - TZ=Europe/Vienna
      - PYTHONUNBUFFERED=1
